-- Довідники → Матеріали → "Використовуються у продукціях": для кастомних
-- полів матеріалу (Довідники → Кастомні поля → Матеріали) додається
-- перемикач "показувати в цій таблиці" — не всі кастомні поля матеріалу
-- доречні в загальній таблиці використання (наприклад, довгий текстовий
-- коментар), тож адмін сам обирає, які колонки туди виводити.
alter table custom_field_definitions
  add column if not exists show_in_material_usage boolean not null default false;
